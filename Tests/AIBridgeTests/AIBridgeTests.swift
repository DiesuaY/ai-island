#if canImport(XCTest)
import XCTest

final class AIBridgeTests: XCTestCase {
    func testPlaceholder() {
        // Bridge is an executable target, hard to unit test directly
        XCTAssertTrue(true)
    }
}
#endif
